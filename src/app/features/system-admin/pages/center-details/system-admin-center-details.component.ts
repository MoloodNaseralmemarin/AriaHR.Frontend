import { ChangeDetectionStrategy, Component, inject } from '@angular/core'; import { ActivatedRoute, RouterLink } from '@angular/router'; import { SystemAdminDataService } from '../../services/system-admin-data.service'; import { CenterStatus } from '../../models/system-admin.models';
@Component({selector:'app-system-admin-center-details',standalone:true,imports:[RouterLink],templateUrl:'./system-admin-center-details.component.html',styleUrl:'./system-admin-center-details.component.css',changeDetection:ChangeDetectionStrategy.OnPush}) export class SystemAdminCenterDetailsComponent { private readonly route=inject(ActivatedRoute); private readonly data=inject(SystemAdminDataService); readonly center=this.data.getCenterById(this.route.snapshot.paramMap.get('id') ?? '') ?? this.data.centers()[0]; getStatusLabel(status:CenterStatus):string{return status==='active'?'فعال':status==='pending'?'در انتظار تکمیل':'غیرفعال';} }
